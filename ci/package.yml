---
platform: darwin
inputs:
  - name: git-roku-radio-com-repo

outputs:
  - name: output

run:
  path: sh
  args:
    - -exc
    - |
      cd git-roku-radio-com-repo
      make pkg
      cd ..
      cp -R git-roku-radio-com-repo/dist/packages/ output/
